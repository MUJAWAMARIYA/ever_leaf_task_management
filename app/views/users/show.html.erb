<h1>Welcome <%= @user.name %> page's</h1>
# <h1><%= @user.name %>page's</h1>

<p>email address: <%= @user.email %></p>
<div class="container">        
        <h3>Add a user</h3>   
       
            <div>   
              <label>user Name:</label>   
              <%= @user.name %>   
            </div>   
  
            <div>   
              <label>email:</label>   
              <%= @user.email%>   
            </div>   
            
            <div>   
              <label>title:</label>   
              <%= @user.title%>   
            </div>   
         
      <div>   
        <%= link_to 'Back', { controller: 'users', action: 'index'} %>   
      </div>   
</div>  
