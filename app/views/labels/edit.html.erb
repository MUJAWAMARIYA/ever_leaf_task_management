<div class="container">   
  
<%= form_for @label, url: {action: :update} do |f| %>   
     
   <h3>Add a labelr</h3>   
 <%= "<div>#{@label.errors.full_messages.first}</div>".html_safe if @label.errors.any? %>   
    <p>   
  
     <div>   
            <label>user Name</label>   
            <%= f.text_field :name %>   
     </div>   
  
     <div>   
            <label>description</label>   
            <%= f.text_field :description%>   
     </div>   
     
      </p>   
   <div>   
      <%= link_to 'Back', { controller: 'labels', action: 'index'} %>   
      <%= f.submit 'Update user' %>   
    </div>   
<% end %>   